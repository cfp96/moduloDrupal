<?php
/**
 * Implements hook_theme().
 */
function example_module_theme($existing, $type, $theme, $path) {
  return [
    'testform' => [
      'render element' => 'form',
      'template' => 'testform',
    ],
  ];
}
